# MIDV-2020 local data

This directory contains acquisition instructions only. The raw MIDV-2020
archives and generated images are intentionally excluded from Git because the
official dataset is approximately 124 GB and access is licence-controlled.

Run `python tools/download_midv2020.py --help` after obtaining provider access.
